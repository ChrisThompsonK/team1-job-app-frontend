# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files with sensitive data
# But allow environment and backend config files
*.tfvars
*.tfvars.json

# Allow environment-specific configuration (no secrets in these)
!dev.tfvars
!prod.tfvars
!staging.tfvars
!backend-*.tfvars

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lock file (some teams commit this, some don't - we'll include it)
# .terraform.lock.hcl
